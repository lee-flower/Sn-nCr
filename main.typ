#import "template.typ": *

#show: project.with(
    title: "等差数列的r阶前n项和与组合数的联系",
    authors: (
        (
            name: "江林锐",
            organization: [梅州市梅县区高级中学],
            email: "d9920713549@163.com"
        ),
    ),
    abstract: "本文的目的在于：用数学归纳法证明了等差数列的r阶前n项和与组合数之间的联系。",
    keywords: (
        "等差数列r阶前n项和",
        "组合数",
        "数学归纳法"
    ),
)

= 定义

== 等差数列${a_n}$的r阶前n项和

设数列${a_n}$为等差数列，其通项公式为$a_n = n$，
现定义$S_n^(\(r\))$为数列${a_n}$的*r 阶前 n 项和*，其中 $r in NN$.
而且对于$forall r >= 1$，都有：

$ display(S_n^(\(r\)) = sum_(i=1)^(n) S_i^(\(r-1\))) $

另外，我们规定：$S_n^(\(0\)) = a_n$.

== 排列、排列数及其计算公式

关于*排列*及*排列数*的定义，详见@pailie .

现给出*排列数*的计算公式：

$ A_n^m = n(n-1)(n-2) dots.h.c (n-m+1) = n!/(n-m)! $

其中，$n, m in NN$且$m <= n$.

== 组合、组合数及其计算公式

关于*组合*及*组合数*的定义，详见@zuhe .

现给出*组合数*的计算公式：

$ binom(n,m) = (A_n^m)/(A_m^m) = n!/(m!(n-m)!) $

其中，$n, m in NN$且$m <= n$.

=== 组合数的一个推论与一个性质

现给出用于本文证明的*组合数*的一个推论和一个性质：

*推论1*：对于任意的$n in NN$，有$binom(n,n)=1$.

*性质1*：对于任意的$n, m in NN_(+)$且$m <= n$，有$binom(n,m) + binom(n,m-1) = binom(n+1,m)$.

对于上述*组合数*的推论和性质的证明，详见@xingzhi .

= 猜想

== 举例

由定义*1.1*可知，

当$r=1$时，$S_(n)^(\(1\))$即$S_(n) = sum_(i=1)^(n) a_i = 1/2 n (n + 1)$；

又如，当$r=2$时，$S_(n)^(\(2\)) = sum_(i=1)^(n) S_i = 1/6 n(n+1)(n+2)$.

#figure(
    image("f1.svg", width:76%),
    caption: [
        绿点、紫点、蓝点分别表示数列${a_n}$、${S_n}$及${S_n^(\(2\))}$
    ]
)

== 猜想等差数列${a_n}$的r阶前n项和的表达式

据*2.1*所举例子，可得如下猜想：

#set math.equation(numbering: "(1)")

$ S_n^(\(r\)) = 1/(r+1)! product_(i=0)^(r) (n+i)
= (n+r)!/((r+1)!(n-1)!) = binom(n+r,r+1) $<chaixiang>

= 证明

现对所做猜想给出证明：

*证*.

(1) 当$r=0$或$r=1$时，显然有 @chaixiang 成立.

(2) 假设当$r=k (k in NN_(+))$时，@chaixiang 成立，即

$ S_n^(\(k\)) = binom(n+k,k+1) $

则据定义*1.1*，有，

$ S_n^(\(k+1\)) & = sum_(i=1)^(n) S_i^(\(k\)) \
& = S_1^(\(k\)) + S_2^(\(k\)) + S_3^(\(k\)) + dots.h.c + S_n^(\(k\)) \
& = binom(1+k,k+1) + binom(2+k,k+1) + binom(3+k,k+1) + dots.h.c + binom(n+k,k+1) $<tuidao3>

由*组合数*的*推论1*可知，@tuidao3 等价于

$ S_n^(\(k+1\)) = binom(2+k,k+2) + binom(2+k,k+1) + binom(3+k,k+1) + dots.h.c + binom(n+k,k+1) $<tuidao4>

又由*组合数*的*性质1*可知，@tuidao4 等价于

$ S_n^(\(k+1\)) & = binom(3+k,k+2) + binom(3+k,k+1) + dots.h.c + binom(n+k,k+1) \
& = binom(4+k,k+2) + binom(4+k,k+1) + dots.h.c + binom(n+k,k+1) \
& dots.h \
& = binom(n+k,k+2) + binom(n+k,k+1) \
& = binom(n+k+1,k+2) = binom(n+(k+1),(k+1)+1) $

即当$n=k+1$时， @chaixiang 也成立.\
由(1)(2)可知， @chaixiang 对任何$r in NN$都成立.

*证毕*

故猜想成立.

= 附录

== 排列及排列数<pailie>

*排列*，一般地，从$n$个不同的元素中取出$m(m<=n)$个元素，
按照一定的顺序排成一列，叫做从$n$个元素中取出$m$个元素的一个排列.
特别地，当$m=n$时，这个排列被称作_全排列_.

*排列数*，排列数指的是从$n$个不同元素中任取$m(m <= n)$个元素排成一列（考虑元素先后出现次序）称此为一个排列，
此种排列的总数即为排列数，即叫做从$n$个不同元素中取出$m$个元素的排列数，记作$A_n^m$.

== 组合及组合数<zuhe>

*组合*，一般地，从n个不同的元素中，任取$m(m<=n)$个元素为一组，
叫作$n$个不同元素中取出$m$个元素的一个组合.

*组合数*，从$n$个不同元素中取出$m(m<=n)$个元素的所有组合的个数，
叫做从$n$个不同元素中取出$m$个元素的组合数，记作$binom(n,m)$.

=== 组合数的一个推论与一个性质的证明<xingzhi>

现给出对*推论1*和*性质1*的证明.

==== 推论1的证明

#set math.equation(numbering: none)

设$n,m in NN$且$m <= n$，

则由*组合数*的计算公式$ binom(n,m) = A_m^n / A_m^m $知，\
当$m=n$时，上式为$ binom(n,n) = A_n^n / A_n^n = 1 $.

故*推论1*得证.

==== 性质1的证明

设$n,m in NN_(+)$且$m <= n$，

则

$ binom(n,m) + binom(n,m-1) & = n!/(m!(n-m)!) + n!/((m-1)!(n-m+1)!) \
& = n!(1/(m(m-1)!(n-m)!) + 1/((n-m+1)(m-1)!(n-m)!)) \
& = n!/((m-1)!(n-m)!) (1/m + 1/(n-m+1)) \
& = n!/((m-1)!(n-m)!) dot.op (n-m+1+m)/(m(n-m+1)) \
& = (n!(n+1))/(m(m-1)!(n-m+1)(n-m)!) \
& = ((n+1)!)/(m!(n-m+1)!) \
& = binom(n+1,m) $

故*性质1*得证.

== 一个有趣的数

当$n=4,r=20$时，$S_n^(\(r\)) = S_4^(\(20\)) = binom(24,5) = 2024$，恰好是本文的写作年份.

在此，本文作者也祝大家2024新年快乐（虽然已经过去快4个月了）！

本文写于*2024*年*3*月*31*日.

== 致谢

在此感谢*钟运辉*老师、*李嘉才*同学、*王富平*同学在本文写作过程中给予的帮助.
